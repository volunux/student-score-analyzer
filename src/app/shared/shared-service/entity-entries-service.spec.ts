import { EntityEntriesService } from './entity-entries-service';

describe('EntityEntriesService', () => {
  it('should create an instance', () => {
    expect(new EntityEntriesService()).toBeTruthy();
  });
});
