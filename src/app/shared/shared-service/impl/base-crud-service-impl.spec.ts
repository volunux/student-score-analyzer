import { BaseCrudServiceImpl } from './base-crud-service-impl';

describe('BaseCrudServiceImpl', () => {
  it('should create an instance', () => {
    expect(new BaseCrudServiceImpl()).toBeTruthy();
  });
});
