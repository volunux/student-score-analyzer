import { EntityFormBuilderService } from './entity-form-builder-service';

describe('EntityFormBuilderService', () => {
  it('should create an instance', () => {
    expect(new EntityFormBuilderService()).toBeTruthy();
  });
});
