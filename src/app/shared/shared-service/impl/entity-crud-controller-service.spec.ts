import { EntityCrudControllerService } from './entity-crud-controller-service';

describe('EntityCrudControllerService', () => {
  it('should create an instance', () => {
    expect(new EntityCrudControllerService()).toBeTruthy();
  });
});
