import { EntityControllerService } from './entity-controller-service';

describe('EntityControllerService', () => {
  it('should create an instance', () => {
    expect(new EntityControllerService()).toBeTruthy();
  });
});
