import { AbstractEntityEntries } from './abstract-entity-entries';

describe('AbstractEntityEntries', () => {
  it('should create an instance', () => {
    expect(new AbstractEntityEntries()).toBeTruthy();
  });
});
