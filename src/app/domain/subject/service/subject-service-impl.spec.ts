import { SubjectServiceImpl } from './subject-service-impl';

describe('SubjectServiceImpl', () => {
  it('should create an instance', () => {
    expect(new SubjectServiceImpl()).toBeTruthy();
  });
});
