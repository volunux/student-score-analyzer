import { SubjectFormBuilderService } from './subject-form-builder-service';

describe('SubjectFormBuilderService', () => {
  it('should create an instance', () => {
    expect(new SubjectFormBuilderService()).toBeTruthy();
  });
});
