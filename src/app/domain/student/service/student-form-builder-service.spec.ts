import { StudentFormBuilderService } from './student-form-builder-service';

describe('StudentFormBuilderService', () => {
  it('should create an instance', () => {
    expect(new StudentFormBuilderService()).toBeTruthy();
  });
});
