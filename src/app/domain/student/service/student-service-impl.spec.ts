import { StudentServiceImpl } from './student-service-impl';

describe('StudentServiceImpl', () => {
  it('should create an instance', () => {
    expect(new StudentServiceImpl()).toBeTruthy();
  });
});
