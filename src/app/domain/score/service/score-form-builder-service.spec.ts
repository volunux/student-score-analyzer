import { ScoreFormBuilderService } from './score-form-builder-service';

describe('ScoreFormBuilderService', () => {
  it('should create an instance', () => {
    expect(new ScoreFormBuilderService()).toBeTruthy();
  });
});
