import { ScoreControllerService } from './score-controller-service';

describe('ScoreControllerService', () => {
  it('should create an instance', () => {
    expect(new ScoreControllerService()).toBeTruthy();
  });
});
