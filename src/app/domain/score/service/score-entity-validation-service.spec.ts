import { ScoreEntityValidationService } from './score-entity-validation-service';

describe('ScoreEntityValidationService', () => {
  it('should create an instance', () => {
    expect(new ScoreEntityValidationService()).toBeTruthy();
  });
});
